!MockEncoderReduce
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality: mode2}
          traversal_paths: [r, c, m]
      - !EncodeDriver
        with:
          method: encode
          granularity_range: [1, 2]
