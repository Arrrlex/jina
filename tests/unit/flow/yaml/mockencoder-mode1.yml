!MockEncoder
requests:
  on:
    IndexRequest:
      - !FilterQL
        with:
          lookups: {modality__in: [mode1]}
          traversal_paths: [r, c, m]
      - !EncodeDriver
        with:
          method: encode
          granularity_range: [1, 2]
